/**
 * @description Exporting the default object.
 * @author Zastinian
 * @param {string} token
 * @param {string} prefix
 */
export default {
  /* It's setting the token to `token`. */
  token: "token",
  /* It's setting the prefix to `!` */
  prefix: "!",
}
