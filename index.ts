/* It's importing the Bot class from the src/Bot.ts file. It's creating a new instance of the Bot class
and calling the init() method on it. */

/* It's importing the Bot class from the src/Bot.ts file. */
import Bot from "./src/Bot";

/* It's creating a new instance of the Bot class and calling the init() method on it. */
new Bot().init();
